# 配置项目数据源信息
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/spring_boot_project?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
# mybatis 相关配置
mybatis:
  mapper-locations: classpath:mapper/*Mapper.xml
  type-aliases-package: com.markus.springboot.project.accumuldation.domain

logging:
  level:
    root: debug

